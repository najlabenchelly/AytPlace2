{% extends 'base.html.twig' %}

{% block title %}Réserver l'annonce : {{ad.title}} {% endblock %}

{% block body %}
<div class="container">
    <h1> Réserver pour l'annonce : {{ad.title}}</h1>
        <p> Vous êtes sur le point de réserver le bien de <strong>{{ ad.author.firstname}}{{ ad.author.lastName}}</strong>
            Emettez un souhait on s'occupe du reste </p>
{{ form_start(form) }}

<div class="alert alert-light">
            <h2 class="h4">Commencez par nous dire quelles sont les dates qui vous intéressent !</h2>
            <div class="row">
                <div class="col">
                    {{form_row(form.startDate)}}
                </div>
                <div class="col">
                    {{form_row(form.endDate)}}
                </div>
            </div>
            </div>
     <h4 class="mt-2">Montant du séjour : <span id="amount"> ...</span></h4>
    <span id ="days"> 0 </span> jours à {{ ad.price }} /jour 
    <h2> Vous avez un commentaire</h2>
    {{ form_row(form.comment) }}
    </div>
    <h2>Vous avez terminé!</h2>
    <p>Pour soumettre votre demande veuillez confirmer votre réservation </p>
    <button type= "submit" class="btn btn-sucess">Je confirme</button>
    {{form_end(form)}}
</div>
{% endblock %}
