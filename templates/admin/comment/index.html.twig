{% extends 'admin/base.html.twig' %}

{% block title %}Administration des commentaires{% endblock %}

{% block body %}
    <div class="container-fluid">
        <h1 class="my-5">Administration des commentaires</h1>

        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Date</th>
                    <th>Auteur</th>
                    <th>Photo</th>
                    <th>Commentaire</th>
                    <th>Note</th>
                    <th>Annonce</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% for comment in comments %}
                <tr>
                    <td>{{comment.id}}</td>
                    <td>{{comment.createdAt|date('d/m/Y H:i')}}</td>
                    <td> {{comment.author.firstName}} {{comment.author.lastName}}</td>
                     <td>
                <img src="{{comment.author.picture}}" alt="" class="avatar avatar-dashboard">
                    </td>
                    <td>{{comment.content}}</td>
                    <td>{{comment.rating}}</td>
                    <td>{{comment.ad.title}}</td>
                    <td>
                        
                        <a href="{{ path('admin_comment_edit', {'id': comment.id}) }}" class="btn btn-primary">
                    <i class="fas fa-pen"></i>
                        </a>
                        <a href="{{ path('admin_comment_delete', {'id': comment.id}) }}" class="btn btn-danger">
                    <i class="fas fa-trash"></i>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>  
    </div>
{% endblock %}
